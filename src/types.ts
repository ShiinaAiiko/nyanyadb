// types?: {
// 	[K in keyof ModelOptionsType]: GetModelOptionsValueType<ModelOptionsType[K]>
// }
// export type DefinitionType<T> = T

// type chatRecordsSchemaType = {
// 	id: number
// 	uid: number
// 	name: string
// 	gender: string
// }

// type ValueOf<T> = T[keyof T]

// type GetModelOptionsValueType<T> = ValueOf<T>
// {Exclude<keyof T, 'default'>
// Exclude<keyof T, 'default'>
// 	[P in keyof MutationType]-?: MutationType[P]
// }
// type GetSchemaTypes<T> = {}
